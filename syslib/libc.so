/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf64-x86-64)
GROUP ( /usr/glibc-compat/lib/libc.so.6 /usr/glibc-compat/lib/libc_nonshared.a  AS_NEEDED ( /usr/glibc-compat/lib/ld-linux-x86-64.so.2 ) )
